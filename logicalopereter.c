#include<stdio.h>
#include<conio.h>
//Logical operater
int main()
{ int a=5,b=10;
 if((a==5)&&(b==10))//For AND opereter,Both candition should be currect
 {printf("Thin will be display");
 }
 if((a==5)||(b==1))//For OR opereter,only one condition should be currect
 {printf("\n Thin will be display");
 if(a!=5) //Not opereter give opposite result
 {printf("\n Thin will be display");
 }
 }
 return 0;

}
